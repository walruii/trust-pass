POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=trust_pass_db

# JWT secret for backend (change in production)
JWT_SECRET=replace_me_with_a_secret

# Frontend should call backend via http://localhost:8000 when using mapped ports
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
